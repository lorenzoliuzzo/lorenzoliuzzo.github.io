_site
.sass-cache
.jekyll-cache
.jekyll-metadata
.bundle
vendor
Gemfile.lock

# MyThingsLab API key — never commit
.env
tools/mythingslab/.env

# Local Claude Code state; .claude/worktrees/ in particular would nest the repo in itself
.claude/

# CV build fonts, fetched by cv/fetch-fonts.sh
cv/fonts/

# MyThingsLab drawer runtime state: the local Engine-spend ledger (shim.py) and
# whatever the fleet's shared venv leaves behind when it imports mythings here.
.mythings/
tools/mythingslab/__pycache__/
